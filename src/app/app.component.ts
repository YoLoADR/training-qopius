import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'app works!';

  // Reproduire un formulaire à la capitain train ou skyscanner
  // Inclure dynamique form

  // Les valeurs du formulaire viennent d'un objet (Semi dynamique)
  // The value of second step is set by the first step
  
  // When we click on GENERATE that's trigger a POST method
  
  // A GET method fetch data and display a Plotly Graph 

}
